{
  "name": "matches-where-query",
  "version": "1.1.4",
  "main": "dst/matchesWhereQuery.js",
  "scripts": {
    "prepublishOnly": "npm install --only=dev && babel src -d lib"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "@rotundasoftware/eslint-config-rotundasoftware": "^2.1.0",
    "eslint": "^5.14.1"
  },
  "dependencies": {
    "underscore": "^1.9.1"
  }
}
